

    let count = 11;
    // document.querySelectorAll("p").innerHTML="Happy Independence Day";
    
    setTimeout(() => {
        count--;
        console.log(count);
        setTimeout(() => {
        count--;
        console.log(count);
        setTimeout(() => {
        count--;
        console.log(count);
        setTimeout(() => {
        count--;
        console.log(count);
        setTimeout(() => {
        count--;
        console.log(count);
        setTimeout(() => {
        count--;
        console.log(count);
        setTimeout(() => {
        count--;
        console.log(count);
        setTimeout(() => {
        count--;
        console.log(count);
        setTimeout(() => {
        count--;
        console.log(count);
        setTimeout(() => {
        count--;
        console.log(count);
        console.log("Happy Independence Day");
        

    }, 1000
    );
}, 1000
    );
}, 1000
    );
}, 1000
    );
}, 1000
    );
}, 1000
    );
}, 1000
    );
}, 1000
    );
}, 1000
    );
}, 1000
    );




// const stopTimeout = () => {
//      countdown(timeoutId);
//  };
   


    
//         let timeoutId = null;
//         timeoutId = setTimeout((
//             document.querySelectorAll("p").innerHTML="Happy Independence Day";

//         }, 10000
//         );

//         const setTimeout = () => {
//             clearTimeout(timeoutId);
//         };
